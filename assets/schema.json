{
  "$schema": "https://frictionlessdata.io/schemas/table-schema.json",
  "name": "odas-app-bussgelder-geschwindigkeitsverstoesse",
  "title": "Bußgelder & Geschwindigkeitsverstöße",
  "fields": [
    {
      "name": "TATTAG",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "TATZEIT",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "TATORT",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "TATBESTANDBE_TBNR",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "GELDBUSSE",
      "type": "string",
      "description": "Wird in der App verwendet",
      "constraints": {
        "required": true
      }
    }
  ]
}
